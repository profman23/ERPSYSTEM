import { Request, Response } from 'express';

export const createBranchCapacity = async (req: Request, res: Response) => {
  // TODO: Implement branch capacity creation logic in Phase 3
  res.status(501).json({ message: 'TODO: Create branch capacity endpoint' });
};

export const getAllBranchCapacities = async (req: Request, res: Response) => {
  // TODO: Implement get all branch capacities logic in Phase 3
  res.status(501).json({ message: 'TODO: Get all branch capacities endpoint' });
};
