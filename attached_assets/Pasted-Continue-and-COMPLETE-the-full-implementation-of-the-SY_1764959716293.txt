Continue and COMPLETE the full implementation of the SYSTEM ADMIN + TENANT ADMIN architecture.

You already started the backend and frontend changes but they are not finished. I need you to finalize ALL pending work. Below is the complete checklist you must implement now:

=====================================================
1. SYSTEM PANEL – USER CREATION FIX
=====================================================
Fix the Create User flow inside /system/users:

- When clicking “Add User” under Platform Users:
  → Show UserTypeSelector modal (System User OR Tenant Admin User)

- If “System User” is selected:
    - Show ONLY: name, email, password, role selector.
    - Roles allowed: SYSTEM_ADMIN only (for now).
    - No tenant / business line / branch fields.
    - Submit to: POST /system/users

- If “Tenant Admin User” is selected:
    - Show ONLY: name, email, password, tenant selector.
    - Auto-assign TENANT_ADMIN role.
    - No business line / branch.
    - Submit to: POST /system/tenant-admins

- Remove the entire Access Scope section from SYSTEM PANEL. It must NOT appear here.

=====================================================
2. SYSTEM ADMIN & TENANT ADMIN ROLE FINALIZATION
=====================================================
Finish the role architecture:

- SYSTEM_ADMIN:
    - Auto-created in SYSTEM tenant.
    - Auto-assigned full permissions ALWAYS.
    - PermissionContext must skip API calls for SYSTEM scope.

- TENANT_ADMIN:
    - Auto-created for every tenant.
    - TENANT_ADMIN should auto-get ALL permissions for that tenant.
    - PermissionContext must return true for all permission checks.

- Both should be marked:
    - isDefault = true
    - isProtected = true

=====================================================
3. ROUTING & SCOPE FIXES
=====================================================
Update all navigation:

- SYSTEM users must stay entirely inside /system/*
- TENANT_ADMIN users must stay inside /admin/*
- Never redirect system users to admin paths.
- Never redirect tenant admins to system paths.

=====================================================
4. COMPLETE BACKEND ENDPOINTS
=====================================================
Finish the system user creation logic:

- POST /system/users → create system-level users
- POST /system/tenant-admins → create tenant admin users
- Validate request schemas
- Assign correct roles automatically
- Fix any missing assignedScope or user-role assignment issues
- Ensure DPF sync is not triggered for built-in roles

=====================================================
5. UI CLEANUP AND CONSISTENCY
=====================================================
Fix CreateUserPage so that:

- It loads UserTypeSelector BEFORE showing any form.
- Forms update dynamically based on user type.
- Wrong fields (tenant, business line, branch) do NOT appear.
- Wrong role dropdowns must NOT appear.
- SYSTEM PANEL must not show admin panel components.

=====================================================
6. DARK THEME – SYSTEM PANEL ONLY
=====================================================
Apply the dark theme shown in /system/dashboard to all SYSTEM screens:

- Tenants
- Add Tenant
- Platform Users
- Add User
- Roles & Permissions
- DPF Manager
- System Settings

No white backgrounds must appear anywhere.

=====================================================
7. VERIFICATION STEPS
=====================================================
Test the following flows:

1. Login as SYSTEM ADMIN →  
   - Try Add User → UserTypeSelector MUST appear

2. Create System User →  
   - Should not ask for tenant or branch.

3. Create Tenant Admin →  
   - Should only ask for tenant selection.

4. Login as Tenant Admin →  
   - Should access ONLY /admin/*
   - Should have full permissions.

5. No more Access Scope UI in SYSTEM PANEL.

=====================================================

After completing everything:
→ Restart server
→ Test all flows
→ Confirm no LSP errors
→ Confirm no schema issues
→ Confirm no routing mistakes

This must now be completed end-to-end.
