You are now responsible for completing Phase 5 Backend Hardening of the ONX CORE multi-tenant SaaS architecture.
Your objective is to achieve absolute, mathematically guaranteed panel isolation across the entire backend.
No route, no controller, and no tenant context may ever leak, even under URL manipulation, expired tokens, multi-tab activity, or spoofed headers.

Proceed with the following tasks in strict order:

1️⃣ HARDEN tenantLoader (CRITICAL)

Rewrite the tenant loader to enforce strict contextual guarantees:

Required Behavior:
System Users

Must ignore and override any tenant-related identifiers.

Must always operate without tenant-bound context.

Cannot be influenced by spoofed headers or URL parameters.

Tenant Admins

Must always operate within their assigned tenant only.

Any mismatch or missing tenant context must result in a security rejection.

Business Line / Branch Users

Must operate strictly within their assigned scope.

tenant, business line, and branch context must always match the authenticated identity.

Any mismatch must result in immediate rejection.

General Rule

If consistent tenant context cannot be resolved, the request must be rejected.

All violations must be logged through a backend audit mechanism.

2️⃣ Replace all implicit or pattern-based panel detection with strict declarative access rules

Eliminate any form of path-based inference, pattern matching, or implicit assumptions.

Required Behavior:

Every backend route must explicitly declare:

Required authentication

Required scope

Required panel identity

No route is allowed to rely on automatic inference.

No backend route is allowed to exist without explicit access rules.

This guarantees future scalability and prevents silent failures.

3️⃣ Introduce a full automated backend test suite using Supertest

Create a complete backend test suite that validates:

System Users

Full access to all system-level functions

Access to administrative and application panels where appropriate

Tenant Admins

Full access only to tenant-level and application functionality

Rejection from any system-level functionality

Application Users

Limited strictly to application-level functionality

Rejection from tenant-level and system-level functionality

Cross-Tenant Isolation

No user should ever be able to access or query data outside their tenant context

All invalid attempts must result in rejection with proper auditing

Token Integrity

Expired tokens must be rejected

Manipulated tokens must be rejected

Missing contextual identifiers must be rejected where required

The test suite must prove isolation, not assume it.

4️⃣ Ensure that no backend route is exposed without:

Authentication middleware

Panel guard enforcement

Scope enforcement

Controller-level business logic validation

The only publicly accessible routes must be limited to the authentication endpoints required for login and token lifecycle.

Everything else must be protected.

5️⃣ Final Deliverables

When complete, produce:

A full list of:

All backend files modified

All vulnerabilities identified and eliminated

All isolation guarantees now enforced

Test results from the automated suite

Your final confirmation must assert:
“Phase 5 Backend Routing & Panel Isolation is now Production-Ready at Enterprise Grade.”