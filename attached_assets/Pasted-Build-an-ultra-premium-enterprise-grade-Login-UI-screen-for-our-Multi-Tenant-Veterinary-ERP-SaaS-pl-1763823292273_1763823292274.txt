Build an ultra-premium, enterprise-grade Login UI screen for our Multi-Tenant Veterinary ERP SaaS platform, using OUR EXACT STACK ONLY.

IMPORTANT:
UI ONLY — no backend, no API calls, no auth logic, no routing logic.
This must be built for global-scale SaaS quality, similar to: Vercel, Linear, Digitail, Stripe Dashboard, Shopify Admin.

=========================================================
🎨 OFFICIAL BRAND COLORS (USE EXACTLY THESE)

--primary: #2563EB;
--primary-dark: #1E40AF;
--primary-light: #60A5FA;
--accent-green: #18AC61;
--neutral-gray: #9CA3AF;
--soft-background: #F3F4F6;
--white: #FFFFFF;

=========================================================

STACK (DO NOT USE ANYTHING ELSE):
- React 18 + TypeScript
- Vite
- TailwindCSS
- shadcn/ui (Card, Input, Label, Button, Separator)
- lucide-react icons only
- i18n-ready (English + Arabic RTL)
- Our global theme.css & globals.css
- Our design tokens and typography
- Zero external UI libraries

=========================================================
UI/UX REQUIREMENTS (ENTERPRISE STANDARD):

1. Global SaaS Layout:
   - Fullscreen responsive layout
   - Desktop: two-column layout with:
       Left: veterinary/medical illustration with a brand overlay
       Right: Login card form
   - Tablet: centered compact card
   - Mobile: stacked full-width form
   - Background: var(--soft-background)

2. Multi-Tenant Ready:
   - Add “Tenant Code” field ABOVE the email field
   - This is UI only (no logic)
   - Controlled component
   - Helps future multi-tenant login flow

3. Branding Area:
   - Logo placeholder at top
   - Use primary blue color #2563EB
   - RTL-compatible

4. Headings:
   Title: “Welcome Back”
   Subtitle: “Sign in to continue”
   Both aligned automatically based on RTL/LTR

5. Input Fields:
   - Tenant Code (text)
   - Email
   - Password (with Eye/EyeOff toggle)
   - Icons: Mail, Lock
   - Focus ring: focus:ring-[#2563EB]
   - Controlled components

6. Security UX:
   - Add “Caps Lock On” subtle warning below password input when caps lock is active
   - Tailwind only (no external libs)

7. Actions:
   - Forgot Password? link
   - Multi-tenant: “Need help?” link (support placeholder)
   - Alignment: right for LTR, left for RTL

8. Login Button:
   - Full width
   - Height: h-11
   - Background: #2563EB
   - Hover: #1E40AF
   - Disabled state included
   - Use shadcn/ui <Button />

9. Micro-Interactions (no animation libraries):
   - Use Tailwind transitions:
       transition-all
       duration-200
       ease-out
   - Inputs: slight scale on focus (scale-[1.01])

10. Accessibility AAA:
   - Proper labels
   - aria-label on all interactive elements
   - High-contrast mode friendly
   - Keyboard accessibility fully supported

11. File Output:
   Create file:
       client/src/pages/auth/LoginPage.tsx

   Export component:
       export default function LoginPage()

12. Future-Proofing:
   - Add invisible placeholder div with data-testid="agi-hook" to attach AGI login assistant later.

=========================================================
IMPORTANT:
Do not implement authentication.
Do not modify other files.
Do not add business logic.
Do not add routing.
Do not connect to backend.

Only generate the UI screen EXACTLY following the enterprise spec above.
