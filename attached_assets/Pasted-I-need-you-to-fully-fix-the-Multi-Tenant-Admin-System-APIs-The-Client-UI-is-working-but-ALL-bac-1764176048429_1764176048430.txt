I need you to fully fix the Multi-Tenant Admin System APIs.  
The Client UI is working, but ALL backend API requests are failing with 401 or 404.

Here are the issues you MUST fix now:

1. The Dashboard shows zero data because all API requests return 401 Unauthorized.
   - Fix JWT verification
   - Ensure the token is included in axios default headers
   - Ensure the AuthContext saves and attaches the token correctly.

2. All hierarchy API endpoints return 401, including:
   - GET /api/v1/tenants
   - GET /api/v1/business-lines
   - GET /api/v1/branches
   - GET /api/v1/tenant/users

   Fix:
   - Auth middleware must accept SuperAdmin token
   - Ensure the backend server loads all routes under /api/v1/*
   - Ensure CORS is enabled correctly

3. Login API is completely broken.  
   The call:
   POST /api/auth/login  
   returns 404 Route Not Found.

   Fix:
   - Ensure the login route exists at /api/auth/login
   - Import the auth routes into server/index.ts
   - Mount them correctly using app.use("/api/auth", authRouter)

4. The frontend expects the backend to run on:
   https://<instance>.kirk.replit.dev:3000/api
   but the backend might not be running on port 3000 or not exposed.

   Fix:
   - Make sure server uses PORT=3000
   - Ensure Replit exposes port 3000
   - Ensure Vite proxy forwards /api → http://localhost:3000/api

5. Business Lines & Branches creation screens do not show any tenants because:
   GET /api/v1/tenants returns 401.

   Once JWT and routes are fixed, confirm that:
   - Tenants list is returned properly
   - Business lines list loads the available tenants
   - Branches list loads business lines

6. Fix all missing routes:
   - /api/v1/tenant/users
   - /api/v1/business-lines
   - /api/v1/branches

7. IMPORTANT:
   After fixing backend routes and token handling, test the system end-to-end:
   - Login works
   - Tenants load
   - Business lines load
   - Branches load
   - Users load
   - Dashboard stats populate correctly

Finally:
Send me a summary explaining exactly what you fixed so I understand the backend changes.