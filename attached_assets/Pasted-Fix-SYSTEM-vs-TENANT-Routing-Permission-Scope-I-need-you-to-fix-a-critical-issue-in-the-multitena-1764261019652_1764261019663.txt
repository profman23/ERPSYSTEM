Fix SYSTEM vs TENANT Routing & Permission Scope

I need you to fix a critical issue in the multitenant RBAC system.

Here is the exact problem with expected behavior:

1) SYSTEM Tenant Behavior (superadmin@system.local
)

When I login as SYSTEM super admin, I am correctly redirected to:

/system/dashboard


But the following issues exist:

❌ Issue A — Wrong navigation on Add User

When I go to:

/system/users


and click Add User, I am redirected to:

/admin/users/create


This is wrong.

✔ Expected:

SYSTEM users must be created at:

/system/users/create


and stored with:

tenant_id = SYSTEM


These users manage the entire platform (all tenants), NOT a single tenant.

❌ Issue B — SYSTEM Roles & Permissions page not working

When I open:

/system/roles


I should see SYSTEM roles, meaning:

Who can create tenants

Who can edit business lines

Who can manage subscription plans

Who can manage platform modules

Who can manage AGI settings

Who can view system-wide analytics

But instead it shows tenant-level behavior or access denied.

✔ Expected:

SYSTEM roles must be loaded from the SYSTEM tenant scope and use SYSTEM permissions only.

2) Tenant Admin Behavior (admin tenant)

When I login as a tenant admin and open:

/admin/roles


I see:

Access Denied – You don’t have permission to view roles.


This is wrong.

✔ Expected:

Tenant admins MUST see:

Tenant-level roles

Tenant-level permissions

Tenant-level module access

Tenant-level users

Branch-based access rules

They should NOT see anything from SYSTEM.

3) Required Fix Summary (Do This Exactly)
✔ A) Separate all SYSTEM routes from TENANT routes

SYSTEM routes = /system/*
TENANT routes = /admin/*

They must never overlap.

✔ B) Fix all Role/Permission queries

Every query should respect:

tenant_id === SYSTEM  (for system pages)
tenant_id === currentTenant (for admin pages)

✔ C) Fix PermissionContext and DPF logic to use the correct tenant scope

Permission loading must distinguish:

SYSTEM super admin permission tree

Tenant admin permission tree

✔ D) Verify Sidebar Rendering

Ensure sidebar items load only the modules available for that tenant level.

✅ Once fixed

Tell me exactly what was changed and confirm SYSTEM and TENANT are fully isolated