I need you to fully implement the missing parts of the DPF (Dynamic Permission Framework) system.
DO NOT generate code directly — create the correct files and write the code yourself inside the Replit workspace.

You must deliver all of the following:

1️⃣ Create a full static DPF Structure

Create a centralized static registry containing:

All top-level modules (≈ 12–16)

All screens inside each module

All actions under each screen (CRUD + custom actions)

Each entry must include:
module_code, module_name, screen_code, screen_name, action_code, action_name

This should be a single source of truth.

2️⃣ Create a DPF Sync Script

Create a script that:

Reads the static DPF structure

Inserts any missing modules, screens, and actions into the database

Updates names if changed

NEVER deletes (safe for production)

Generates permissions automatically for each action

Is 100% idempotent (safe to run many times)

Logs exactly what was created or updated

Script name:

server/src/db/seed/seedDPFStructure.ts

3️⃣ Integrate Sync into SYSTEM tenant

Modify the startup flow so that:

After seedSuperAdmin runs

Run the DPF Sync Script for SYSTEM tenant

SYSTEM tenant must always have:

All modules

All screens

All actions

All permissions generated

No exceptions.

4️⃣ Add Auto-Sync on Server Startup

On server startup:

Detect schema changes between static DPF structure and DB

Auto-run the sync script

Log all changes to console (clean readable format)

This ensures future modules and screens automatically appear in DB without manual migration.

5️⃣ Validate whole RBAC pipeline end-to-end

After implementing:

Validate modules exist in DB

Validate screens exist

Validate actions exist

Validate permissions generated

Validate SYSTEM tenant has them

Validate tenant-scoped logic still works

Validate PermissionContext receives full data

Return a final audit confirming everything is correct.

6️⃣ Notes & Rules

Do NOT break tenant isolation

Do NOT delete or modify existing data

Sync must be additive only

All logic must be written in TypeScript (strict mode)

Ensure perfect compatibility with PermissionContext and RBAC UI

This must scale to 2000+ tenants seamlessly

🔚 End of prompt

Execute everything automatically.
Generate files and code on your own.
Return a summary when done.