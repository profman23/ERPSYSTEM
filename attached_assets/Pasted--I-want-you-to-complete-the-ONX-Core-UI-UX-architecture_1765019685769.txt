**"
I want you to complete the ONX Core UI/UX architecture to the world-class standard used in companies like Uber, Meta, WhatsApp, and SAP.

Your job now is to finish implementing the Global Theming Engine + Component Library + Multi-Tenant Branding according to the following strict requirements:

🔥 1) GLOBAL THEMING ENGINE (Final Production Architecture)

Implement a full three-layer token architecture:

Layer 1 — Global Semantic Tokens

Defined once in globals.css:

--color-bg
--color-surface
--color-text
--color-accent
--color-muted
--color-border
--radius
--shadow
--sidebar-bg
--sidebar-active
--header-bg
--button-primary
--button-primary-hover
--button-secondary
--input-bg
--input-border
--input-focus

No component should use raw Tailwind colors again.

Layer 2 — Component Tokens

Every component in /client/src/components/ui MUST use tokens only:

Button
Card
Input
Select
Dialog
Table
Badge
Sidebar
Header
Tabs
Forms

All styling MUST be token-based and support dynamic theme inheritance.

Layer 3 — Panel Themes (Controlled by data-panel)

Implement 3 panel themes:

SYSTEM PANEL → Dark Purple (enterprise management)
ADMIN PANEL → Light Blue (tenant admin management)
APP PANEL → Medical Teal (clinic operations)

Themes must load automatically using:

<html data-panel="system"> <html data-panel="admin"> <html data-panel="app">

And cascade tokens accordingly.

🔥 2) MULTI-TENANT BRANDING LAYER (GLOBAL STANDARD)

Implement tenant-level theme overrides using:

tenant.settings.theme = {
primary,
secondary,
background,
surface,
accent,
radius,
logo,
sidebarStyle
}

When tenant logs in → automatically injects CSS variables for that tenant.

This must support:

✔ 3000 tenants
✔ each with different branding
✔ zero performance impact
✔ fully dynamic theme switching
✔ no build-time CSS generation

🔥 3) REMOVE ALL HARDCODED COLORS ACROSS THE ENTIRE APP

Search the entire client folder and remove:

bg-white
bg-gray-100
text-black
text-gray-*
border-gray-*
any hex colors (#fff, #000, etc.)

Replace ALL with token-based classes.

🔥 4) FIX ALL WHITE SECTIONS IN SYSTEM / ADMIN PANELS

All pages must fully inherit their panel theme:

SYSTEM pages → Zero white. Only system theme.
ADMIN pages → Zero white. Only admin theme.
APP pages → Zero white. Only app theme.

Every page in:

/system/*
/admin/*
/app/*

must visually match its panel theme instantly.

🔥 5) ROUTING + SCOPE FIXES (CRITICAL)

Fix the access flow:

SYSTEM ADMIN → always stays in /system/*
TENANT ADMIN → always stays in /admin/*
NORMAL USERS → always stay in /app/*

No tenant admin should ever redirect to /app.
Fix the Tenant Admin login routing immediately.

🔥 6) CODE QUALITY & ARCHITECTURE STANDARDS

All UI code must follow:

✔ Token-driven design system
✔ Zero duplication
✔ Perfect panel theming isolation
✔ Perfect tenant branding override
✔ Component Library First architecture
✔ Extensible for mobile apps + micro frontends later
✔ Ready for AGI UI auto-generation in the future

Deliverables:

Updated globals.css with full token engine

Refactored Component Library (all UI components)

Updated System/Admin/App layouts with data-panel attributes

Updated all pages to remove hardcoded colors

Tenant branding loader (injects CSS variables at login)

Routing fixes for panels

Show me a summary of all files changed