We need to build the full Platform Core Layer for our Multi-Tenant Veterinary ERP SaaS (Node.js + Express + Drizzle + AsyncLocalStorage + Redis + Neon PostgreSQL + React).

This layer must follow the highest global standards used by AWS, Stripe, Uber, Shopify, Netflix, and Plaid.

The Platform Core Layer must guarantee:

unlimited scalability (2,000+ tenants to 10,000+ in the future)

high performance under heavy load

strong observability & debugging

zero technical debt

clean extensibility for future clinical/financial modules

long-term system stability and maintainability

Build the following components in this exact order:

1) Request Context Layer (CRITICAL – Implement First)

Use AsyncLocalStorage to create a full context object per request containing:

traceId (UUIDv7 or ULID)

correlationId

requestId

tenantId

userId

branchId

clientIp

forwardedFor

userAgent

deviceFingerprint

timestamp

requestStart / requestDuration

✔ Auto-inject into Winston logs
✔ Auto-attach to Prometheus metrics
✔ Auto-attach to Drizzle query logs
✔ Auto-propagate to Socket.IO events
✔ Include sampling rules (1%, error-based, adaptive)
✔ Support parentSpan / distributed tracing structure
✔ Support propagation through outbound HTTP requests
✔ Support propagation into background jobs (BullMQ)

2) Health Check & Readiness Probes

Implement:

/health (basic liveness)

/health/ready (DB, Redis, Queue, Socket.IO, etc.)

/health/live (Kubernetes-style)

Return system metrics via Prometheus

This ensures the platform is observable and ready for scaling.

3) Audit Logging System (High Priority)

Create an enterprise-grade audit log system:

Write logs asynchronously

Store traceId + tenantId + userId

Old/New JSON diff

Action type (create/update/delete/login/permission-change/etc.)

Resource type & resourceId

IP + userAgent

Error context

Must support:

"Who did what?"

"When?"

"From where?"

"What changed exactly?"

Row-level audit trails

Schema:
audit_logs(id, tenant_id, user_id, action, resource_type, resource_id, old_data, new_data, ip, ua, trace_id, created_at)

4) API Versioning Structure

Organize all routes under:

/api/v1/*


Prepare infrastructure for:

/api/v2/* later

Header-based versioning

Graceful deprecation

Backward compatibility

This ensures future clinical modules do not break existing tenants.

5) Global Rate Limiting Layer (Tenant + User + IP)

Implement:

per-tenant rate limit

per-user rate limit

per-IP rate limit

burst protection

sliding window / token bucket algorithms

Redis-based rate limiting

Return structured error:
429 Too Many Requests + traceId + retry-after.

This prevents abuse and keeps the platform stable under load.

6) Global Caching Strategy (Tiered Cache)

Implement multi-layer caching:

L1 (in-memory per request)

L2 (Redis cache for hot data)

L3 (DB query caching with Stale-While-Revalidate)

Cache tagging per tenant

Auto-invalidation hooks

TTL policies

This reduces DB load by 70%, speeds up the system, and improves scalability.

7) Multi-Tenant Quotas & Limit Policies

Add resource quotas per tenant:

max-users

max-branches

max-patients

max-appointments

max-api-requests-per-day

plan-level quota tiers (Basic, Pro, Enterprise)

Integrate quota failures with:

Audit logs

Request context

Alerts (later)

8) (Optional Later) Internal Event Bus

Not needed right now, but prepare an architecture compatible with:

appointment.created

patient.updated

inventory.low

billing.generated

labresult.received

Use a minimal interface now for future expansion.

Final Deliverables:

Provide:

Full folder structure

All code files for each layer

Middleware integration

Logging integrations

Usage examples in controllers/services

Drizzle query logging with context

Socket.IO context propagation

Rate limiting + cache examples

Quota enforcement examples

Health check implementation

Versioned API router factory

Full documentation (README)

Build it as if it were the observability/safety layer for a global enterprise SaaS platform.