I want you to complete the missing implementation for the ONX Multi-Tenant ERP System.  
Focus on fixing the remaining SYSTEM PANEL + TENANT PANEL issues, theme unification, and full role isolation.

## ✅ PART 1 — SYSTEM PANEL (Full Completion)

1) Fix System Panel Theme (Dark Mode ONLY)
- Ensure every page under /system/* uses the System Admin dark theme
- Remove ALL white backgrounds from:
  - /system/tenants
  - /system/users
  - /system/users/create
  - /system/roles
  - /system/dpf
  - /system/settings
  - /system/metrics
- Unify all components so System Panel uses:
  --sys-bg
  --sys-surface
  --sys-border
  --sys-text
  --sys-accent

2) Platform Users Fix
- When clicking “Add User” → show UserTypeSelector:
    ● System User
    ● Tenant Admin User
- If System User → create SYSTEM-level user, assign SYSTEM_ADMIN role automatically
- If Tenant Admin User → show *only tenant selector*, NOT branches or business lines
- Ensure Role dropdown only shows:
    ● System Admin
    ● Tenant Admin  
  (no tenant roles inside System Panel)

3) System Roles Page
- Should only display:
    ● SYSTEM_ADMIN (protected)
    ● SYSTEM_SUPPORT (optional)
    ● BILLING_SUPPORT (optional)
- TENANT_ADMIN roles must NOT appear here.

4) Routing & Scope Isolation (CRITICAL)
Fix the incorrect routing behavior:
- SYSTEM users must ALWAYS stay inside /system/*
- TENANT ADMIN users must ALWAYS stay inside /admin/*
- Regular tenant users must ALWAYS stay inside /app/*

Currently Tenant Admin redirects to /app/dashboard → FIX THIS.

Scope rules:
if user.accessScope == "system" → basePath = "/system"
if user.accessScope == "tenant" → basePath = "/admin"
if user.accessScope == "branch" → basePath = "/app"

Update:
- scopeGuard.ts
- ScopedRedirect.tsx
- LegacyRedirect
- useScopePath()

## ✅ PART 2 — TENANT PANEL (Admin Dashboard Fixes)

1) Fix Tenant Admin Routing
Tenant admins must land on:
    /admin/dashboard
NOT /app/dashboard.

2) TENANT PANEL Theme
- Keep the light theme for now BUT ensure:
    ● No white blocks appear incorrectly
    ● Buttons, cards, tables use unified design
- Use same UI tokens but with different colors:
    --tenant-bg
    --tenant-surface
    --tenant-border
    --tenant-accent

3) Add Required Fields to Create Tenant Page
When creating a tenant from SYSTEM PANEL, add fields:
- Allowed Business Lines (number)
- Allowed Branches (number)
- Subscription Plan (Basic / Pro / Enterprise)

**These fields must be stored in the tenant record** and passed into the Tenant Admin Panel.

4) Tenant Admin User Experience
Ensure Tenant Admin can:
- Create business lines (up to allowed limit)
- Create branches (up to allowed limit)
- Create tenant-level users (doctor / receptionist / nurse / accountant)
- Assign roles (DPF roles)
- Manage tenant settings safely

## ✅ PART 3 — UI/UX Unification Across the System

1) BUTTONS
Make all buttons consistent:
- Primary = purple accent (#8B5CF6)
- Secondary = dark grey
- Danger = red

2) CARDS + PANELS
Standardize Cards:
- Rounded-xl
- Border using token
- Background using surface token

3) FORMS
All forms must use:
- Standard spacing
- Standard labels
- Standard Input + Select components without white background

## ✅ PART 4 — BUG FIXES & VALIDATION

Fix the following:
- SYSTEM Admin login should never fail due to accessScope reset
- Tenant Admin should not lose routing context
- Ensure TENANT_ADMIN and SYSTEM_ADMIN roles are seeded with:
    isDefault: true
    isProtected: true
    wildcards enabled
- Ensure auto-permissions for SYSTEM_ADMIN & TENANT_ADMIN work everywhere

## ✅ PART 5 — FINAL REVIEW CHECKLIST

When done, validate that:

✔ SYSTEM PANEL has 0% white background  
✔ System Panel Users flow works end-to-end  
✔ Tenant Admin creates business lines + branches with limits  
✔ Tenant Admin stays in /admin/*  
✔ System Admin stays in /system/*  
✔ Regular tenant users stay in /app/*  
✔ Role behavior is correct and does not leak  
✔ Theme is unified and professional  
✔ Buttons, cards, forms all consistent  
✔ Fully scalable for 2000+ tenants  

Proceed with full implementation now.
