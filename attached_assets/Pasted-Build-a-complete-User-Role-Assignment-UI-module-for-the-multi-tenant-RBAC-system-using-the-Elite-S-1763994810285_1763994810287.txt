Build a complete “User Role Assignment UI” module for the multi-tenant RBAC system using the Elite Stack architecture (React + TypeScript + Shadcn + React Query + DPF).
Implement this EXACT specification with maximum reliability, scalability, and AGI-level automation:

🎯 1. PURPOSE — WHAT WE ARE BUILDING

Create a complete production-grade UI that allows Tenant Admins to:

View all users

Assign roles to users

Remove roles from users

See role impact before applying changes

See permission conflicts

Multi-role assignment

Read/write via React Query

Auto-sync with backend via Socket.IO

Fully permission-controlled (DPF)

Multi-tenant safe (uses tenant context)

Supports 50,000 users / 2,000 tenants

📁 2. FILES TO CREATE

client/src/pages/admin/UserRoleAssignmentPage.tsx

client/src/components/roles/UserRoleAssignmentDrawer.tsx

client/src/components/roles/RoleImpactPreview.tsx

client/src/components/roles/UserRolesTable.tsx

client/src/hooks/useUserRoles.ts

Add route /users/:userId/roles to AppRouter

Update UsersPage to add “Manage Roles” button

🧠 3. CORE FEATURES TO IMPLEMENT
✅ A. User Role Assignment Drawer (RIGHT SIDE PANEL)

Sliding drawer (Shadcn Drawer or Dialog)

Shows:

User email

Full name

User status

List of all roles with checkboxes

Selected roles load from:

GET /api/tenant/users/:id/roles

Saving:

POST /api/tenant/users/:id/assign-role

POST /api/tenant/users/:id/remove-role

💡 AGI MODE:

Add inline natural-language interpreter:

Example:

Admin types:
“Give this user all finance permissions”

AGI resolves:
→ Find all Finance module roles
→ Auto-select roles
→ Show preview before applying

Add button: “Interpret AI Command”

✅ B. Role Impact Preview (SUPER ENTERPRISE FEATURE)

When roles change:

Calculate BEFORE permissions

Calculate AFTER permissions

Show:

1) Gained permissions
2) Lost permissions
3) Conflicting permissions
4) High-risk permissions (DELETE, FINANCE_APPROVE, etc.)

Display in a diff viewer UI.

✅ C. Permission Conflict Detection

If assigned roles give:

MODIFY + READ_ONLY conflict

FINANCE_APPROVE without FINANCE_VIEW

ADMIN.role.modify without ADMIN.role.view

Show red warning:

“⚠️ Permission Conflict Detected
This user will receive unbalanced privilege escalation.”

✅ D. Real-Time Updates with Socket.IO

Listen for events:

user-role-updated
role-updated
permissions-updated


Auto-refresh UI WITHOUT reload.

✅ E. Search / Filter / Pagination

Search users by name/email

Filter users by role

Sort by (name, status, created_at)

Backend already optimized with 81 indexes → safe.

⚙️ 4. API INTEGRATIONS

Use React Query:

GET

/api/tenant/users

/api/tenant/users/:id/roles

/api/tenant/roles

/api/tenant/permissions/all

POST

assign role

remove role

Optimistic updates with rollback.

🔐 5. FULL PERMISSION CONTROL (DPF)

Only show this page if user has:

roles.assign
roles.view
users.view


Only allow saving if:

roles.assign

🧩 6. COMPONENT ARCHITECTURE
UserRoleAssignmentPage

Reads userId from route

Loads user + roles + permissions

Shows AssignDrawer + ImpactPreview

UserRoleAssignmentDrawer

List of allowed roles

Role toggles

Save / Reset

RoleImpactPreview

BEFORE vs AFTER permissions

Diff viewer

UserRolesTable

For future: mass-assignment

🔁 7. STATE MANAGEMENT RULES

Use:

React Query for fetching

Zustand or Context for local drawer state

Set operations for permission diffs

🧪 8. TEST CASES (must add)

Assign single role

Assign multiple roles

Remove role

Rapid toggle (10 times quickly)

Offline → Sync when online

Socket live update

Conflict detection

Role impact preview correctness

Large dataset (10,000 users)

Must pass all tests.

💾 9. PERFORMANCE REQUIREMENTS

UI must handle 50,000 users without freeze

Must virtualize long lists

Must paginate

Must memoize permission calculations

Zero-lag diff preview

🛡️ 10. MULTI-TENANT SAFETY REQUIREMENTS

Always pass tenantId from backend

Never load roles from other tenants

Never merge permissions cross-tenant

All requests must include JWT with tenant context

If tenant mismatch → throw fatal error.

🎨 11. UI/UX REQUIREMENTS

Use Shadcn components

Use your theme (medical blue / white)

RTL support for Arabic

Smooth animation for drawer

Badges for role types

Icons (Lucide):

User

Shield

Key

Check

AlertTriangle

🧠 12. AGI-ENHANCED FEATURES (MANDATORY)

Auto-fill suggestions:

“Users similar to this user have Role X”

“Recommended roles based on user’s department”

“This user is missing FINANCE_VIEW for FINANCE_UPDATE”

AGI permission reasoning engine:

Given natural text → compute role operations

📝 13. DELIVERABLES

Replit must deliver:

All 5 components

Updated router

Updated UsersPage

Working UI

Saving to backend

Hydration from backend

Socket auto-sync

Role impact diff system

Conflict detection engine

Full DPF integration

AGI interpreter commands

RTL support

Production-level error handling

📌 14. OUTPUT REQUIREMENTS

All file paths exact as written

Zero TypeScript errors

Zero ESLint errors

Production-ready code

Fully wired to backend

All buttons + flows functional

Architect verification must PASS