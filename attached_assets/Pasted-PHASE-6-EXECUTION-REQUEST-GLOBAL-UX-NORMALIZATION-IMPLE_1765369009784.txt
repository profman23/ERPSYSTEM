PHASE 6 — EXECUTION REQUEST (GLOBAL UX NORMALIZATION IMPLEMENTATION)
I have reviewed the full Phase 6 Architecture Document you generated (PHASE6_UX_NORMALIZATION_ARCHITECTURE.md).
Now I want you to begin the FULL EXECUTION of Phase 6 according to the blueprint.

Important notes before you start:

Do NOT write experimental code.

Follow the architecture document strictly.

Apply enterprise-grade consistency, scalability, and maintainability standards.

Every change must follow the global token system, panel theming rules, and unified component strategy.

No hardcoded colors, spacing, or typography values should remain anywhere in the codebase.

Your Execution Objectives:
1. Implement Global UX Consistency Across All Pages (31+ pages)

Migrate every page from Tailwind colors + inline styles
→ into the global design token system described in the architecture.

No exceptions.

This includes:

Auth pages (Login, Reset)

System panel pages

Tenant panel pages

App panel pages

CRUD pages

Dashboard pages

Tables, lists, detail screens, modals, forms

2. Apply the Unified Component Strategy

Ensure ALL UI elements use the standardized APIs defined in Phase 6:

variant

size

tone

state

panelTone

No custom classes, no overrides except as tokens.

3. Normalize All Layouts

Unify:

Sidebar width

Border radius scale

Spacing grid

Header/footer padding

Section wrappers

Form layouts

Panels must feel like three different branded systems,
but all follow the same structural logic.

4. Implement the Unified UI State System

Create and apply:

<LoadingState />

<EmptyState />

<ErrorState />

Each must adapt dynamically to the active panel using tokens only.

Replace all ad-hoc loaders, empty blocks, error texts.

5. Remove ALL Hardcoded Colors and Tailwind Color Classes

Replace ALL occurrences of:

hex colors

bg-gray-*

text-red-*

bg-white

inline style colors

with token-based equivalents defined in the architecture file.

This is mandatory for:

Light/dark mode future expansion

Brand overrides per tenant

RTL/LTR typography consistency

Multi-theme support (enterprise clients)

6. Fix All Typography Inconsistencies

Apply:

font tokens (AR + EN switching logic)

heading scale

body scale

consistent weight hierarchy

No manual text-[xx] classes unless token-based.

7. Prepare the System for Future Dynamic Branding

Your Phase 6 implementation must allow:

per-tenant primary & accent colors

automated WCAG contrast checks

theme cascade overrides

runtime theme switching

Do NOT implement branding UI now — only prepare the system for it.

8. Execute the Work in Waves (As Defined in the Document)

Follow the recommended execution plan:

Wave 1: Auth + Tenant core pages

Wave 2: System panel pages

Wave 3: Admin + App panel pages

After each wave:

Run a visual audit

Confirm no hardcoded colors left

Confirm component consistency

Confirm panel theme integrity

Confirm token-based behavior

Final Requirements

When you finish each wave, provide me with:

A list of changed files

A summary of what was improved

Before/After behavioral notes

Any discovered inconsistencies

Any recommended adjustments

A visual screenshot if possible

Execution Quality Standard

Your implementation must match:

Meta’s Design System (FB5 UI)

Uber Base Web System

Stripe Dashboard Design Tokens

SAP Fiori UX Consistency Rules

Precision, consistency, and scalability are mandatory.