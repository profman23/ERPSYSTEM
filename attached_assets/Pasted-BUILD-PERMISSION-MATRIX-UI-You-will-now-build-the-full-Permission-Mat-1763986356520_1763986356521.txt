BUILD PERMISSION MATRIX UI
==========================

You will now build the full Permission Matrix UI for the RBAC system.

⚠️ IMPORTANT CONTEXT
The following foundational work is already completed:
- DPF Structure (14 modules, 22 screens, 85 actions)
- seedDPFStructure auto-sync on startup
- PermissionContext fully implemented (14 helper methods + 5 specialized hooks)
- Role CRUD UI fully implemented
- Role Permissions API and service ready
- PERFORMANCE: 81 database indexes → ready for production scale
- FRONTEND: Shadcn components installed and ready (Table, Accordion, Checkbox, Dialog, etc.)

Your mission is to build the **Permission Matrix UI** under:
client/src/pages/roles/RolePermissionsPage.tsx
and integrate it inside:
RolesPage → "Manage Permissions" button

==========================================
🎯 TARGET FEATURES (DELIVER EXACTLY)
==========================================

1. **Hierarchical Permission Tree**
   Module → Screen → Action hierarchy based on:
   GET /api/tenant/permissions/matrix

   Structure Example:
   - Administration
     - Roles & Permissions
       - create
       - view
       - update
       - delete

2. **UI Layout**
   Use Shadcn Accordion + Nested Checkboxes:
   - First level: Modules (expandable)
   - Second level: Screens (expandable)
   - Third level: Actions (checkbox list)

3. **Two-Way Binding**
   When user toggles:
   - Action checkbox → updates role
   - Screen checkbox → toggles all children actions
   - Module checkbox → toggles all screens & actions

4. **React Query Integration**
   Use:
   - usePermissionMatrix()
   - useRolePermissions(roleId)
   - mutate() to POST updated permissions

   After saving:
   - invalidate ['roles']
   - invalidate ['permissions']
   - invalidate ['rolePermissions', roleId]

5. **Permission Checks**
   Use PermissionContext helper methods:
   - hasAction()
   - hasPermission()
   - canUpdate('roles')
   - canView('roles')

6. **Save Button Behavior**
   - Disabled unless changes were made
   - Spinner while saving
   - Toast notifications (success/error)

7. **File Structure**
   Create:
   - client/src/pages/roles/RolePermissionsPage.tsx
   - client/src/components/permissions/PermissionMatrix.tsx
   - client/src/components/permissions/PermissionCheckbox.tsx
   - client/src/components/permissions/PermissionTree.tsx

8. **Routing**
   Add new route:
   /admin/roles/:roleId/permissions

9. **Navigation**
   In RolesPage row actions:
   - Add button: “Manage Permissions”
   - Navigates to RolePermissionsPage

==========================================
💡 REQUIREMENTS & RULES
==========================================

- DO NOT modify backend controllers or services.
- DO NOT modify PermissionContext logic.
- Use only existing APIs.
- Respect Shadcn UI patterns.
- Use Suspense + ErrorBoundary for lazy-loaded components.
- Follow the established code style & folder structure.
- Must support large datasets (85+ actions).
- Must be fully tenant-isolated.
- No hardcoded permissions.
- No direct DPF structure in frontend — always fetch from API.

==========================================
🎯 ADDITIONAL REQUIREMENTS
==========================================

- Implement checkAll / uncheckAll per module
- Implement checkAll / uncheckAll per screen
- Real-time counter: “Selected X of Y actions”
- Display Arabic labels if translation exists in DPF data
- Sticky header with Save + Cancel buttons
- Auto-sync when switching roles
- Optimized rendering (React.memo where needed)

==========================================
🚀 EXPECTED OUTPUT
==========================================

Deliver:
- All UI files
- Updated routes
- Updated RolesPage actions
- Fully tested Permission Matrix UI
- Fully working saving/updating permissions
- No TypeScript errors
- No runtime errors
- Zero breaking changes

After completion, provide:
- Summary of all created/updated files
- Screenshots of rendered UI (if possible)
- Notes about performance or design improvements
