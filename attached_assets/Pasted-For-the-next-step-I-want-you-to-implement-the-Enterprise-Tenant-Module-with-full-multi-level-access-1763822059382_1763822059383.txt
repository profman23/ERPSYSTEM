For the next step, I want you to implement the Enterprise Tenant Module with full multi-level access scope support.

IMPORTANT:
Do NOT implement Users, Roles, Permissions, or RBAC logic yet.
Only implement the Enterprise Tenant Module + Access Scope structure.
Do NOT create sample data. Do NOT create default users.

===============================
🔥 1. ENTERPRISE TENANT MODULE (BUILD NOW)
===============================
Implement the following schema structure using Drizzle (no business logic yet):

TENANTS TABLE:
- id (uuid)
- code (string, unique)
- name (string)
- default_language (string: "en" | "ar")
- country (string, optional)
- timezone (string, optional)
- created_at
- updated_at

BUSINESS_LINES TABLE:
- id (uuid)
- tenant_id (fk)
- name (string)
- code (string)
- description (optional)
- logo_url (optional)
- primary_color (optional)
- secondary_color (optional)
- accent_color (optional)
- is_active (boolean)
- created_at

BRANCHES TABLE:
- id (uuid)
- tenant_id (fk)
- business_line_id (fk)
- name (string)
- code (string)
- city (string)
- address (optional)
- is_active (boolean)
- created_at

BRANCH_CAPACITY TABLE:
- id (uuid)
- tenant_id (fk)
- business_line_id (fk)
- branch_id (fk)
- allowed_users (int)

===============================
🔥 2. MULTI-LEVEL ACCESS SCOPE (STRUCTURE ONLY — NO AUTH YET)
===============================
Add the data structure for future access control:

In the USERS TABLE (structure only, do NOT build logic yet):
- access_scope: "system" | "tenant" | "business_line" | "branch"
- tenant_id (nullable)
- business_line_id (nullable)
- branch_id (nullable)

Do NOT create any user logic, tokens, or RBAC now.
Only define the table fields so Phase 3 can use them.

===============================
🔥 3. API ENDPOINTS (STRUCTURE ONLY)
===============================
Create the following empty API endpoints (no logic yet):

Tenants:
- POST /api/tenants
- GET /api/tenants
- GET /api/tenants/:id

Business Lines:
- POST /api/business-lines
- GET /api/business-lines
- GET /api/business-lines/:id

Branches:
- POST /api/branches
- GET /api/branches
- GET /api/branches/:id

Branch Capacity:
- POST /api/branch-capacity
- GET /api/branch-capacity

Do NOT add business logic.
Only create controllers with TODO placeholders.

===============================
🔥 4. FRONTEND (STRUCTURE ONLY)
===============================
Create empty pages and components:

- /tenants (list page)
- /tenants/create
- /business-lines
- /business-lines/create
- /branches
- /branches/create

Leave all pages as placeholders with TODO components.

===============================
🔥 5. IMPORTANT RULES
===============================
❗ Do NOT implement users
❗ Do NOT implement authentication
❗ Do NOT implement permissions
❗ Do NOT implement RBAC/ABAC
❗ Do NOT generate sample data
❗ Do NOT add actual logic — only architecture

I only want the Enterprise Tenant Module + Multi-Level Access Scope STRUCTURE ready for Phase 3.

Begin now.
