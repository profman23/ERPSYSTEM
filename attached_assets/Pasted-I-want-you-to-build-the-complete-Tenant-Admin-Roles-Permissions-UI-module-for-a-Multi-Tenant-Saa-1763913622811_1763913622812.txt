I want you to build the complete “Tenant Admin Roles & Permissions UI” module for a Multi-Tenant SaaS Veterinary ERP system using the existing DPF-AGI (S-Tier) permission framework.

This module is for TENANT ADMIN only (not Super Admin).  
It must fully integrate with:
- dpf_roles
- dpf_permissions
- dpf_role_permissions
- dpf_user_roles
- dpfEngine
- permissionMiddleware
- tenant isolation (AsyncLocalStorage)

──────────────────────────────────
🎯 MAIN OBJECTIVE:
──────────────────────────────────
Build a full end-to-end module that allows each clinic (tenant) to:

1) Create Roles  
2) Edit Roles  
3) Delete Roles (if no users are assigned)  
4) Manage Permissions per Role (Matrix UI)  
5) Assign Roles to Users  
6) Enforce permissions on the frontend (UI hiding + ProtectedRoute)  
7) Enforce permissions on the backend (permissionMiddleware)  

This UI must work ONLY inside a tenant context and must NOT show cross-tenant data.

──────────────────────────────────
🟦 PART 1 — ROLES MANAGEMENT UI (Tenant Level)
──────────────────────────────────

Create UI pages/components:

1) **Roles List Page**
- Table showing: name, description, usersCount, createdAt
- Buttons: Create Role, Edit, Delete
- Search + Filters
- Pagination
- Must load data via React Query from: GET /api/tenant/roles

2) **Create/Edit Role Modal**
Fields:
- name
- description
- isProtected (read-only)
- isDefault (checkbox, only one default per tenant)

3) **Delete Role**
- Allowed only if usersCount == 0
- Confirmation dialog

4) **Role Details Page**
Tabs:
- Assigned Users
- Permissions Matrix
- Role Metadata

──────────────────────────────────
🟩 PART 2 — PERMISSION MATRIX UI
──────────────────────────────────

Build a world-class Permission Matrix using:

- Modules → Screens → Actions
- Each action is a checkbox
- Group Permissions automatically by module + screen
- Toggle checkbox → updates dpf_role_permissions
- Select All for a module
- Select All for a screen

Use Shadcn UI + Accordions + Checkboxes + Grids.

APIs:
- GET /api/tenant/permissions
- GET /api/tenant/roles/:id/permissions
- POST /api/tenant/roles/:id/permissions

──────────────────────────────────
🟨 PART 3 — ASSIGN ROLE TO USERS
──────────────────────────────────

Inside Staff Management:

- For each user → Assign Role
- Show list of tenant roles
- Allow selecting 1 active role
- Save via endpoint:
  POST /api/tenant/users/:id/assign-role

Ensure updates reflect in UI using React Query invalidate.

──────────────────────────────────
🟥 PART 4 — BACKEND ROUTES (REQUIRED)
──────────────────────────────────

Implement ALL backend endpoints under:
server/src/routes/tenant/roles/
server/src/routes/tenant/permissions/
server/src/routes/tenant/users/

Endpoints:

GET /api/tenant/roles  
POST /api/tenant/roles  
PATCH /api/tenant/roles/:id  
DELETE /api/tenant/roles/:id  

GET /api/tenant/permissions  
GET /api/tenant/roles/:id/permissions  
POST /api/tenant/roles/:id/permissions  

GET /api/tenant/users  
POST /api/tenant/users/:id/assign-role  

ALL must:
- use permissionMiddleware
- enforce tenant isolation
- use Drizzle ORM with tenantId where required
- validate payload using Zod
- return structured JSON results

──────────────────────────────────
🟪 PART 5 — FRONTEND PERMISSION ENFORCEMENT
──────────────────────────────────

Integrate:
- usePermissions() hook
- checkPermission("roles.view")  
- <ProtectedRoute required="roles.view" />

Hide UI elements if lacking permission:
- hide Create Role if no: roles.create
- hide Edit Role if no: roles.update
- hide Delete Role if no: roles.delete
- hide permission matrix if no: permissions.assign

──────────────────────────────────
🟧 PART 6 — AGI/VOICE FUTURE STRUCTURE
──────────────────────────────────

Prepare structure but do not implement fully:

Examples:
- “Give doctor full medical permissions”
- “Block receptionist from deleting patients”
- “Show all POS-related permissions”

Implement placeholder hooks:
client/src/hooks/useAgiPermissions.ts  
server/src/rbac/agiInterpreter.ts (extend patterns)

──────────────────────────────────
🏁 FINAL OUTPUT REQUIRED:
──────────────────────────────────

I want:

✔ Full backend routes  
✔ Full UI pages (React + Shadcn)  
✔ All Role/Permission management components  
✔ Permission Matrix UI  
✔ Hooks (useRoles, usePermissions, useMatrix)  
✔ Tenant isolation fully enforced  
✔ Integration with DPF Engine  
✔ Clean file structure  
✔ Production-ready TypeScript code  

Start implementing this module NOW.
